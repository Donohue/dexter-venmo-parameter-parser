/*
 * Metadata describing the module operation
 */
{
    //Required. Inputs the module expects.
    "inputs": [
        //e.g.
        {
            "id": "message",
            "title": "Message",
            "description": "Message must have a format like \"@<Venmo Username> $<Amount>\". Those values are parsed and become outputs.",
            "type": "string"
        }
    ],
    "outputs": [
      //e.g.
        {
            "id": "username",
            "title": "Venmo Username",
            "type": "string"
        },
        {
            "id": "amount",
            "title": "Amount to Pay",
            "type": "integer"
        }
    ],
    "title": "Donohue Venmo Parameter Parser" /* Required.A display name for the module */
}
